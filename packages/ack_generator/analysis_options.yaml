# Inherits the shared workspace config (lints + DCM presets/rules).
# See /analysis_options.yaml at the workspace root.
include: ../../analysis_options.yaml

analyzer:
  errors:
    # Suppresses deprecations from the EXTERNAL `analyzer` package (mid-migration
    # from the `Element2` API back to `Element`). Not related to Ack's own API,
    # which has no deprecations. Remove once ack_generator migrates off Element2.
    deprecated_member_use: ignore

linter:
  rules:
    # Generator-specific relaxations for codegen-heavy code.
    prefer_single_quotes: true
    avoid_print: false
    lines_longer_than_80_chars: false
    avoid_classes_with_only_static_members: false
